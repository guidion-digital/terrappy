name: Release

on:
  push:
    branches:
      - master

jobs:
  # release new tag for the workflow
  release-module-version:
    uses: guidion-digital/release-workflows/.github/workflows/github-release-tag.yaml@release-workflows
    permissions:
      contents: write
